# 依赖
node_modules

# Next.js 构建产物
# 注意：.next/ 此前被误纳入版本控制；新增忽略后需执行 `git rm -r --cached .next` 取消跟踪
.next/
out/
*.tsbuildinfo

# 环境变量
.env
.env.*
!.env.example

# 系统文件
.DS_Store

# 工具产物（critique 快照等）
.impeccable/

# Claude 本地工具配置（skills / agents / 设置，不随仓库公开）
.claude/

# 内部文档（不随仓库公开）
DEVELOPMENT_PLAN.md
FEATURES.md
AGENTS.md
mvp/
